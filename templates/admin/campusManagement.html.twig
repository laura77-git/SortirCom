{% extends 'base.html.twig' %}
{% block body %}
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <a class="navbar-brand" href="{{ path('home_connected') }}">Accueil</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a href="{{ path('city_management') }}" class="nav-link">Villes</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('myProfile') }}" class="nav-link">Mon profil</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('security_login') }}" class="nav-link">Se déconnecter</a>
                </li>
            </ul>
        </div>
    </nav>
    <div class="jumbotron text-center">
        <h1>Sortir.com</h1>
    </div>
    <h2 style="text-align: center;">Gérer les campus</h2>
    <br>
    <div class="container">
        <h4>Filtrer les sites</h4>
        <div class="row">
            <label>Le nom contient : </label>
            <input type="search">
            <button type="submit" class="btn btn-primary" value="">Rechercher</button>
        </div>
        <br>
        <table class="table table-striped">
            <thead>
            <tr>
                <th>Campus</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            <tr>
                <td></td>
                <td></td>
                <td>
                    <a href="#">Modifier</a>
                    <a href="#">Supprimer</a>
                    <a href="#">Ajouter</a>


                </td>

            </tr>
            </tbody>
        </table>
    </div>
{% endblock %}
